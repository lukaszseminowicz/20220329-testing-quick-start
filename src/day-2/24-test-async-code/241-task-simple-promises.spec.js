/**
 *
 * Promises przetestujemy sobie w oparciu, o wycinek programowania obiektowego.
 * Razem z UserService, przechowującym stan.
 *
 *
 * Zaprojektuj service zwracający użytkowników w postaci Promises.
 *
 * - Metoda .getAll() - zwraca promise z wszystkimi użytkownikami.
 * - Metoda .getUserById() - zwraca promise z jednym użytkownikiem, jeśli istnieje — lub błąd, jeśli go nie ma.
 * - Metoda .addUser() - zwraca promise z informacją o powodzeniu dodania użytkownika
 *
 * Napisz odpowiednie testy, przygotuj osobny moduł dla UserService
 * */

class UserService {
  users = [];

  getAll() {}

  getUserById() {}

  addUser() {}
}

it.todo('should have some test here...')
